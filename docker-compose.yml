version: "3.9"

services:
  app:
    build: .
    ports:
      - "${PORT:-3000}:3000"
    environment:
      - PORT=3000
      - DB_PATH=/app/data/app.db
      - UPLOAD_DIR=/app/data/uploads
    volumes:
      - ./data:/app/data
