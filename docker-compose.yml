version: "3.9"

services:
  app:
    build: .
    ports:
      - "${PORT:-3010}:3010"
    environment:
      - PORT=3010
      - DB_PATH=/app/data/app.db
      - UPLOAD_DIR=/app/data/uploads
    volumes:
      - ./data:/app/data
